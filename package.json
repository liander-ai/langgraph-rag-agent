{
  "name": "langgraph-rag-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A retrieval-augmented (RAG) question-answering agent built as a LangGraph state machine",
  "scripts": {
    "ask": "tsx src/run.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@langchain/anthropic": "^0.3.11",
    "@langchain/core": "^0.3.30",
    "@langchain/langgraph": "^0.2.39"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
